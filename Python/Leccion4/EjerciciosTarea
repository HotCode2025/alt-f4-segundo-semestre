#Ejercicio 1: llenar una lista
#con los números del 1 al 50,
#mostrar la lista con el bucle For
#los elementos deben mostrarse de la siguiente forma: 1-2-3-4-5......50

numeros1 = []
for i in range(1, 51):
    numeros1.append(i)

    resultadolista = "-".join(map(str, numeros1))
    print(resultadolista)

#Ejercicio 2: Modificar los elementos de una lista
#llenar una lista con los números del 1 al 10
#luego modificar los elementos multiplicandolos por un valor ingresado por el usuario

numeros2 = list(range(1, 11))

multiplicador = int(input('Ingresar un número para multiplicar los números de lista: '))

for i, numero in enumerate(numeros2):
    numeros2[i]  = numero * multiplicador

print('La nueva lista es :', numeros2)

#Ejercicio 3: Insertar elementos y ordenarlos
#pedir numeros y meterlos en una lista y cuando el usuario introduzca el numero cero
#nuestro programa dejara de insertar
#por último mostrar los numeros de mayor a menor

numeros = []
print('Introduce numeros aleatorios entre 1 y 10 (ingresa cero para finalizar): ')
while True:
    try:
     entrada = int (input('Introduce numeros: '))
     if entrada == 0:
      break
     numeros.append(entrada)
    except ValueError:
        print('entrada no valida, ingrese un numero: ')

numeros.sort(reverse=True)
print('Los números de mayor a menor son: ', numeros)